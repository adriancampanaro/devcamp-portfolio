<%= image_tag @portfolio_item.main_image unless @portfolio_item.main_image.nil? %>

<h1><%= @portfolio_item.title %></h1>

<em><%= @portfolio_item.subtitle %></em>

<p><%= @portfolio_item.body %></p>

<h2>Technolgoies Used:</h2>

<%= @portfolio_item.technologies.each do |t| %>
	<p><%= t.name %></p>
<% end %> 

<hr>
<!--<%= params.inspect %>
<br>
	<%= params[:id] %>  ---- <%= params[:action] %>
<br>
<%= params.inspect %>

<br>

<%= session.inspect %>
-->
<!--<%= Portfolio.find(params[:id]) %>  ---- <%= Portfolio.find(params[:id]).title %>---- <%= Portfolio.find(params[:id]).technologies %>-->